# 阶段图说明与 QA（2026-09-26）

配套 [完整结果报告](../docs/STAGE_RESULTS_20260926.md)。图源为 `data/processed/stage_20260926`，由公开效应表重算。三张组图不是最终正文图号；文件名里的 Stage 仅用于本轮交付定位。

## Stage 1

左：105 条处理—对照产量效应，浅色小点为单条年度/剂量/季节记录；深色大点为试验内平均 lnRR 转为百分比。直接还田 9、生物炭 6、焚烧 9 个试验—路径组合，合计来自 18 个不同试验，不把共享试验重复计算。右：25 个试验—路径—作物组合；同一轮作试验可出现在两个作物中。横向是效应，纵向小幅错开仅用于显示，不表示时间、概率或误差。未作检验，未画 CI；不允许据跨路径点云作因果排名。

来源表：`screened_effects.csv`（yield 子集）、`trial_responses.csv`、`yield_by_crop.csv`；所有记录保留 `effect_id` 或 `effect_ids`。

## Stage 2

左：15 个产量—SOC 浓度匹配对来自 4 个试验；右：42 个产量—土壤 GWP 匹配对来自 3 个试验。记录必须在试验、路径、作物、年度、季节、处理臂、对照臂、水分及氮标签上匹配；非唯一键不做笛卡尔拼接。匹配的是文献均值记录，不是原始小区观测，尚无跨终点协方差。空白季节可在明确年度内匹配，不能因此声称观测日在逐日尺度完全一致。

颜色标路径，形状标试验。同一颜色多个点可能全部来自一个试验。左侧不混入 SOM 代理或总碳未明确记录，不把不同土层强行配成多个独立产量观测。右侧是土壤排放口径，未合并 AR4/AR5 因子、未纳入完整燃烧与生物炭生命周期；看单试验内相对响应，不能量化全路径气候净收益。没有回归、独立性假设检验、P 值或多重比较。

来源表：`joint_outcome_pairs.csv`，每个点有 `yield_effect_id`、`endpoint_effect_id` 和 DOI；未匹配记录见 `joint_matching_audit.csv`。图中 Tai Lake trial 对应 `biochar_li2024SD_3`，Mono-rice trial 对应 `rice_primary_53`，完整题名/DOI 在公开源表。

## Stage 3

左：直接还田相对焚烧，10 个对比来自 4 个试验键；右：生物炭相对直接还田，28 个对比来自 2 个试验。明确共享对照且均值相同后相减 lnRR，指数还原为两处理的比例差。菱形为每个试验内对比的平均 lnRR 转为百分比，小点为各年度/剂量对比，横线为最小—最大观测范围，不是 SE/SD/CI。多个生物炭剂量可共用一个直接还田处理，未据此增加独立试验数。剂量/碳投入不一定等量，两个 Bhubaneswar 试验还可能共享站点条件。未作显著性或排名推断。

来源表：`matched_pathway_pairs.csv`、`matched_pathway_trials.csv`；保留双方源效应 ID。0 表示两处理均值相同，正值表示横轴冒号后前者高于后者。

## 文件与拆分

- 组图：`stage1_yield`、`stage2_joint`、`stage3_matched_paths`。
- 独立面板：相同前缀加 `_left` / `_right`。
- 每项有 PNG（600 dpi 预览）、PDF、SVG（可编辑文字）；组图 183 mm 宽，拆分只改变页面边界，不改字号或缩放面板。拆分 PDF 不包含另一个面板的页外隐藏内容。
- 用户要求优先：无图标题、无面板编号，粉/蓝/紫来自指定 palette。右侧作物面板另用符号区分路径，联合图符号区分试验。

## 已执行检查

| 面板 | 独有解释作用 | 中心/离散定义 | 样本单位 | 布局与标签 | 结果 |
| --- | --- | --- | --- | --- | --- |
| Stage 1 左 | 响应方向及试验内/间变化 | 全部记录 + 试验内平均 lnRR，无 CI | 试验；浅点为重复记录 | 与右轴等宽、等高、齐边 | 通过 |
| Stage 1 右 | 识别作物构成不均 | 试验—作物均值，无 CI | 试验—作物，存在共享试验 | 符号/颜色和坐标核查 | 通过 |
| Stage 2 左 | 产量与碳浓度是否同向 | 全部可匹配记录，无拟合 | 同试验记录对 | 零线、SOC 度量、试验图例核查 | 通过 |
| Stage 2 右 | 增产与土壤排放代价 | 全部可匹配记录，无拟合 | 同试验记录对 | 保留反向点，未裁剪高排放点 | 通过 |
| Stage 3 左 | 同背景比较还田与焚烧 | 试验内中心 + 全部观测范围 | 试验；两个试验同站点 | 不把横线标为 CI | 通过 |
| Stage 3 右 | 显示生物炭排名反转 | 同左 | 仅两个试验 | 两种方向均保留 | 通过 |

三张组图均保留 `*.alignment.json`：1.5 pt 容差的等宽、等高、边界对齐检查通过。九个 PDF（3 组图 + 6 拆分）最小文字 6.5 pt，字体审计通过；最终碰撞审计均 0 FAIL / 0 WARN，JSON 与文件同目录。已逐面板及整图目视检查；不使用不透明白块遮盖冲突文字。

源码审计 20 PASS / 1 WARN / 0 FAIL。唯一 WARN 是未导出 TIFF；本轮交付为阶段分析，已提供 PDF/SVG 向量图和 PNG 预览，不宣称满足某期刊最终投稿栅格格式要求，因此保留该提示。自动图形检查不验证原文提取、统计推断或全球代表性。
