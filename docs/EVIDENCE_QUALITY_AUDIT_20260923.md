# 正式证据快照的方差来源与旧等级审计

日期：2026-09-23。范围仅限公开表中原先带 `legacy_analysis_tier_pending` 的 52 条效应；不是所有 428 条的全面偏倚风险评价。一个田间试验为独立单位，多剂量、跨年或多结局不增加独立试验数。

## 结论与处置

| 原始试验键 | 行数 | 已有全文核验记录 | 本次处置 |
| --- | ---: | --- | --- |
| `biochar_li2024SD_31` | 6 | 本地 `data/formal_analysis_v1/validate_study_31.py`：只准入 Fig. 2 稻谷产量；原文说明 SD、n=3，图形数字化；6 条误标为 SOC 的候选记录已隔离。 | 校正旧 `pending` 标签；保留图形提取限制和旧标签。当前 DOI 仍缺失，不猜测补填。 |
| `biochar_li2024SD_71` | 12 | `validate_study_71.py`：Shen 2014，Table 5 产量和 Fig. 3a/3b 气体；原文 SE 按 n=3 转为 SD，剔除重复原料记录，修复 N₂O 单位/小数位。 | 校正为原文核验且 SE 换算层级；保留共享对照、跨季重复及图形数字化说明。 |
| `biochar_li2024SD_73` | 12 | `validate_study_73.py`：Peng 2021，Table 2 的 2015–2018 年 SOC 均值±SD、n=3；未定义误差条的产量记录仍在 held 层。 | 校正为原文表格 SD 层级；保留重复年份/共享对照。 |
| `biochar_li2024SD_176` | 14 | `validate_study_176.py` 已核对田间处理，但公开行仍是二手 SD/n；逐行来源不明且缺表图定位。 | **不校正方差来源**；`variance_origin_status=unresolved_row_origin`，首轮逆方差模型计数排除。 |
| `biochar_li2024SD_3` | 8 | `validate_study_3.py` 已核对无氮层配对；公开行仍含逐行二手方差来源不明标记且缺表图定位。 | 同样保留待核查；全文准入决定不能替代逐行方差证明。 |

公开表新增 `source_analysis_tier`，保留来源表旧值；`analysis_tier` 是经试验键、结局、原文定位和方差描述签名核对后的当前等级，`tier_reconciliation` 说明校正。`variance_origin_status` 仅区分逐行方差来源是否明确：`documented_or_reconstructed` **不等于**通过了共享对照、误差重建或系统边界的全部主模型审核。校正对象的源行签名变化时，导出脚本报错而非静默套用旧规则。

## 对有效研究数的影响

以下是**所有公开层 → 排除 22 条逐行方差来源不明后的筛查层**，不是最终 Meta 模型准入数：

| 生物炭结局 | 所有效应 / 独立试验 | 方差来源筛查后效应 / 独立试验 |
| --- | ---: | ---: |
| 产量 | 44 / 6 | 38 / 4 |
| SOC | 38 / 6 | 34 / 4 |
| CH₄ | 28 / 4 | 22 / 2 |
| N₂O | 28 / 4 | 22 / 2 |

直接还田和露天焚烧各格子的本次计数不变；唯一达到预设“≥10 项独立试验”**计数**门槛的仍是露天焚烧×产量（19 条 / 10 项）。这不替代系统边界、设计依赖、方差重建与影响诊断。

## 仍需完成的原文检查

1. 对研究 176 和 3，逐条建立 `effect_id → 原文表/图/补充资料 → 处理臂均值 → SD/SE 类型 → n` 对照表；确认二手数据是否混入插补方差。不能核实时，保留均值证据，不从主题相近的论文补数。
2. 对研究 31 核实 DOI（目前 6 行缺失）并保留数字化敏感性；对研究 71 的图形提取气体结果安排整项剔除敏感性；对研究 73 不把 held 产量直接晋级。
3. 重新评估每个 `路径 × 结局` 的独立试验数和共同支持域；过门槛且结构一致才运行首轮模型。

## 可复核命令

```bash
python scripts/export_evidence_database.py --check
python scripts/describe_evidence.py
python -m unittest discover -s tests -v
```

公开快照可复核本次标记和计数，但原始全文及 196 列核验源表默认仍在本地，不宣称公众可从仓库独立重做逐篇抽取。本次没有改变处理/对照均值、SD、n、`lnRR`、方差或试验键；仅校正公开等级和方差来源门槛的表达。
